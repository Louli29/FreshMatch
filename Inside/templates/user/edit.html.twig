{% extends 'Main.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block main %}
    <h1 class="text-center">Modifier mes informations</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}

    <div class="text-center mt-4 pb-4">
        <a href="{{ path('app_user_account', {'id': user.id}) }}" class="btn btn-primary">
            Retourner sur mon compte
        </a>
    </div>
{% endblock %}
